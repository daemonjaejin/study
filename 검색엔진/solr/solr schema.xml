<field name="itemName" type="string" indexed="true" stored = "true" multiValued="true" />

multiValued -> true 설정 : 1개 이상을 배열로 return 받을 수 있다.
indexed -> 설정 : index 여부
type -> 설정 : 문자, 숫자, 지역, 텍스트 등등
stored -> 설정 : 저장여부
name -> 설정 : db-config.xml에 설정한 이름